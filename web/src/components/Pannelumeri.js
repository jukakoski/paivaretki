import React from 'react'
import { Pannellum } from "pannellum-react";

/* export default function Pannelummeri(props) {
    // custom component using shopify client-side libraries
    return <div>
        <Pannellum {...props} />
    </div>
} */

export default Pannellum